---
id: T-012
title: Creating a task, focus task
status: done
assignee: null
priority: null
tags:
  - ux
  - enhancement
depends_on: []
created_at: 2026-04-25
updated_at: 2026-04-26
---

## Goal

When a task is created, i would like it to be selected in the Kanban directly when returning to the board. TUI

## Acceptance Criteria

## Notes

## Progress

- 2026-04-26: Started. Confirmed scope with user — when filter/search would hide the new task, clear them; always return focus to board after create.
- 2026-04-26: Implemented in `packages/cli/src/tui/App.tsx` (`onCreate`): after `createTask` + reload, clears filter to `{kind: "all"}` and `searchQuery`, sets focus to `board`, then re-fetches via `listTasks`, locates the new task's column (`statuses.indexOf(task.status)`) and row index within that column, and calls `setColumnIndex` / `setRowIndex` / `ensureRowVisible` so the new task is selected and scrolled into view. Verified with `pnpm typecheck` and `pnpm test` in both `cli` and `core` (28 tests passing). Done.
