---
id: T-015
title: Kanban edit warning
status: done
assignee: null
priority: null
tags:
  - web
  - ui
depends_on: []
created_at: 2026-04-26
updated_at: 2026-04-26
---

## Goal

When closing the kanban edit using the edit icon or close icon should result in a dialog warning the user that the content of the task is not saved. Give the option to cancle, don't save or save. This is the web ui

## Acceptance Criteria

## Notes

## Progress

- 2026-04-26: Started. Confirmed scope with user — warn only when draft is dirty; trigger on edit icon, X, Cancel button, Escape and scrim click; dialog with Save / Don't save / Cancel (Esc=Cancel).
- 2026-04-26: Implemented in `packages/web/src/client/TaskModal.tsx`. Added `isDirty` (JSON-compare draft vs saved baseline), `pendingClose` state, and a small `UnsavedChangesDialog` rendered as a sibling scrim. All four close paths (edit-icon, X, Cancel, Esc + scrim click) route through `requestClose`, which gates on dirty. Esc inside the dialog cancels; Enter saves. `pnpm test` (9 passing), `tsc --noEmit` and `vite build` all clean.
