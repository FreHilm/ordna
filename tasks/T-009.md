---
id: T-009
title: Manage External Kanban (Linear)
status: todo
assignee: null
priority: medium
tags:
  - integration
  - linear
  - plugin
depends_on:
  - T-006
  - T-018
  - T-023
  - T-008
created_at: 2026-04-25
updated_at: 2026-05-01
---

## Goal

Ship `@frehilm/ordna-linear` reusing the patterns proven by `@frehilm/ordna-jira` (T-008). Same TUI / Web UI, swappable backend, configured via `.ordna/config.yaml`.

This is **Phase 3** of T-018. Read-only first, write follows.

## Acceptance Criteria

### Read-only milestone (`@frehilm/ordna-linear@0.1.0`)

- [ ] New package `packages/linear/` with `src/{index,provider,client,schema,map}.ts`
- [ ] Exports `createProvider(config, cwd): TaskProvider` per the contract from T-022
- [ ] Auth via env var (e.g. `LINEAR_API_KEY`) — Linear uses simple API-key auth
- [ ] Config block in `.ordna/config.yaml`:
  ```yaml
  provider: linear
  linear:
    apiKeyEnv: LINEAR_API_KEY
    teamId: <team-id>
    filter: "state.type IN (started, unstarted)"
  ```
- [ ] Implements `list`, `get`, `watch` via Linear's GraphQL API
- [ ] `kind === "linear"`
- [ ] `Task.remote` populated: `provider: "linear"`, `externalId: <issue-id>`, `url: <issue-url>`, `extras` with cycle / project / parent issue
- [ ] Field mapping per the table below
- [ ] `watch` prefers GraphQL subscriptions when WebSocket is available; falls back to polling
- [ ] Tests: unit-test mapping; integration tests stub the Linear GraphQL endpoint
- [ ] `packages/linear/README.md`
- [ ] Reuses field-mapping helpers / `Task.remote.extras` convention introduced by T-008 (don't duplicate the schema-translation utilities — extract to a shared location if needed)

### Write milestone (`@frehilm/ordna-linear@0.2.0` — separate task or follow-up)

- [ ] Implements `create`, `update`, `move`, `delete` via Linear mutations
- [ ] Writes restricted to the same fields supported in T-008's write milestone

## Notes

### Field mapping

Linear's model is closer to Ordna's than Jira's, so the table is shorter:

| Ordna `Task` field         | Linear field                  | Direction |
|----------------------------|-------------------------------|-----------|
| `id`                       | Linear identifier (`ENG-42`)  | r/w |
| `title`                    | `title`                       | r/w |
| `status`                   | `state.name`                  | r/w |
| `assignee`                 | `assignee.displayName`        | r/w |
| `priority`                 | `priority` (1-4) → `high`/`medium`/`low`/null | r/w |
| `tags`                     | `labels.nodes[].name`         | r/w |
| `depends_on`               | `relations` where `type == "blocks"` (inverse) | r/w |
| `sections`                 | `description` (markdown native — no translation) | r/w |
| `remote.extras.cycle`      | `cycle.name`                  | r |
| `remote.extras.project`    | `project.name`                | r |
| `remote.extras.parentIssue`| `parent.identifier`           | r |

### Why depends on T-008

T-008 lands the field-mapping conventions, the `Task.remote.extras` blob shape, the polling-watcher pattern, and the rate-limit handling. T-009 is largely a different transport over those same primitives. Building T-009 second avoids repeating those decisions and lets us move shared helpers into core (or a common subpackage) if patterns emerge.

### Real-time

Linear has GraphQL subscriptions over WebSocket — first-class real-time updates without polling. v1 should use subscriptions when available, polling as fallback (e.g. behind a corporate firewall that blocks WS).

## Progress
